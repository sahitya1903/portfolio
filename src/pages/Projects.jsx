import SectionPage from '../components/layout/SectionPage';
import ProjectsArchive from '../sections/projects/ProjectsArchive';

/** /projects — the full, filterable project archive. */
const Projects = () => (
  <SectionPage>
    <ProjectsArchive />
  </SectionPage>
);

export default Projects;
