import SectionPage from '../components/layout/SectionPage';
import ContactSection from '../sections/contact/ContactSection';

/** /contact — the contact form as a standalone page. */
const Contact = () => (
  <SectionPage>
    <ContactSection />
  </SectionPage>
);

export default Contact;
