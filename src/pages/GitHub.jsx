import SectionPage from '../components/layout/SectionPage';
import GitHubSection from '../sections/github/GitHubSection';

/** /github — the open-source footprint section as a standalone page. */
const GitHub = () => (
  <SectionPage>
    <GitHubSection />
  </SectionPage>
);

export default GitHub;
